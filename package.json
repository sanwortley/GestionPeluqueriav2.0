{
    "name": "roma-cabello-fullstack",
    "version": "1.0.0",
    "scripts": {
        "postinstall": "npm run install:all",
        "install:all": "concurrently \"npm run install:client\" \"npm run install:server\"",
        "install:client": "cd frontend && npm install",
        "install:server": "cd server && pip install -r requirements.txt && alembic upgrade head && python -m app.seed.seed_admin && python -m app.seed.seed_services",
        "dev": "concurrently -k --names \"SERVER,CLIENT,BRIDGE\" -c \"magenta,cyan,blue\" \"npm run dev:server\" \"npm run dev:client\" \"npm run dev:bridge\"",
        "dev:server": "cd server && python -m uvicorn app.main:app --reload --port 8001",
        "dev:client": "cd frontend && npm run dev",
        "dev:bridge": "cd whatsapp-bridge && npm start"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}